[package]
name = "tuiman-indexer"
description = "Builds the tuiman index from awesome-tuis; runs in CI"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
toml = "1.0.7"
tuiman-index = { path = "../tuiman-index" }
ureq = { version = "3", features = ["json"] }

[lints]
workspace = true
